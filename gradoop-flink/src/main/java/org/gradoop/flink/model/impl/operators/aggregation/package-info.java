
/**
 * Aggregation takes a logical graph and a user defined aggregate function as
 * input. The aggregate function is applied on the logical graph and the
 * resulting aggregate is stored as an additional property at the result graph.
 */
package org.gradoop.flink.model.impl.operators.aggregation;
